SELECT 
    {[[Measures].[Academics FactCount]]} ON COLUMNS,
    {[Social Media Dim].[Category].Members} ON ROWS
FROM [Star Schema Cube]
WHERE ([Social Media Dim].[Category].[All]);

SELECT 
    {[Measures].[Study Time], [Measures].[Social Media Time], [Measures].[Cgpa]} ON COLUMNS, 
    {[College Dim].[College Name].Members} ON ROWS
FROM [Star Schema Cube]
